import { Logo } from './logo';
import { NavbarContainer } from './navbar.styles';

export const NavBar = () => {
    return (
        <NavbarContainer>
            <Logo />
        </NavbarContainer>
    );
};